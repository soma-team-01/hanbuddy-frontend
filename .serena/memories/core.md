# Core

- Hanbuddy frontend: tourist-to-buddy activity booking service.
- Remote: `origin` = `git@github.com:soma-team-01/hanbuddy-frontend.git`.
- Source root: `src/`; App Router pages under `src/app`; route groups `(tourist)`, `(buddy)` are organizational only and not URL segments.
- Figma implementation source of truth: Hanbuddy GUI page, fileKey `wzlRJND1GMNskVuydcWpns`, page node `2038:269`. Use individual screen node IDs from AGENTS.md rather than page-wide metadata when possible.
- Shared UI invariants and route/screen mapping live in AGENTS.md; consult before implementing screens or navigation.
- Read `mem:tech_stack` for framework/version constraints, `mem:conventions` for design/code conventions, `mem:suggested_commands` for local commands, and `mem:task_completion` before claiming a coding task is complete.
